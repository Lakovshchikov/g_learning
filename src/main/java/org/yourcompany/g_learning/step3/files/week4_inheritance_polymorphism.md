# 📚 Неделя 4: Наследование и полиморфизм

## 🎯 Цель недели

Понять принципы наследования и полиморфизма, научиться проектировать иерархии классов и эффективно переиспользовать код.

---

## 📅 День 1: Введение в наследование

### Теоретическая часть (1.5 часа)

#### Что такое наследование?

- Концепция наследования в ООП
- Отношение "является" (is-a relationship)
- Ключевое слово `extends`
- Родительский класс (superclass) и дочерний класс (subclass)

#### Базовый синтаксис

```java
class Animal {
    String name;
    int age;

    void makeSound() {
        System.out.println("Животное издает звук");
    }
}

class Dog extends Animal {
    String breed;

    void wagTail() {
        System.out.println("Собака виляет хвостом");
    }
}
```

#### Что наследуется?

- Публичные и защищенные поля
- Публичные и защищенные методы
- Что НЕ наследуется: конструкторы, приватные члены

### Практические задания (1.5 часа)

**Задача 1.1:** Создать класс `Vehicle` (транспортное средство) с полями `brand`, `year`, `maxSpeed` и методом `displayInfo()`. Создать класс `Car`, наследующий `Vehicle`.

**Задача 1.2:** Создать класс `Employee` с полями `name`, `id`, `salary` и методом `work()`. Создать класс `Manager`, наследующий `Employee`, и добавить поле `teamSize`.

**Задача 1.3:** Создать класс `Shape` с полем `color` и методом `getColor()`. Создать классы `Circle` и `Rectangle`, наследующие `Shape`.

---

## 📅 День 2: Конструкторы в наследовании

### Теоретическая часть (1.5 часа)

#### Ключевое слово `super`

- Вызов конструктора родительского класса
- `super()` должен быть первой строкой в конструкторе
- Неявный вызов `super()` по умолчанию

#### Примеры

```java
class Animal {
    String name;

    Animal(String name) {
        this.name = name;
    }
}

class Dog extends Animal {
    String breed;

    Dog(String name, String breed) {
        super(name);  // Вызов конструктора Animal
        this.breed = breed;
    }
}
```

#### Цепочка конструкторов

- Порядок вызова конструкторов в иерархии
- Инициализация полей родителя и потомка

### Практические задания (1.5 часа)

**Задача 2.1:** Доработать классы из Дня 1: добавить конструкторы с параметрами и использовать `super`.

**Задача 2.2:** Создать класс `Person` с конструктором. Создать класс `Student extends Person` с дополнительными полями `studentId` и `course`. Правильно использовать `super`.

**Задача 2.3:** Создать класс `BankAccount` с полями `accountNumber` и `balance`. Создать класс `SavingsAccount extends BankAccount` с полем `interestRate`. Реализовать конструкторы.

---

## 📅 День 3: Переопределение методов

### Теоретическая часть (1.5 часа)

#### Переопределение (Override)

- Что такое переопределение метода
- Аннотация `@Override`
- Правила переопределения
- Разница между переопределением и перегрузкой

#### Примеры

```java
class Animal {
    void makeSound() {
        System.out.println("Животное издает звук");
    }
}

class Dog extends Animal {
    @Override
    void makeSound() {
        System.out.println("Гав-гав!");
    }
}

class Cat extends Animal {
    @Override
    void makeSound() {
        System.out.println("Мяу!");
    }
}
```

#### Вызов метода родителя через `super`

```java
class Employee {
    void work() {
        System.out.println("Работает");
    }
}

class Manager extends Employee {
    @Override
    void work() {
        super.work();  // Вызов метода родителя
        System.out.println("Управляет командой");
    }
}
```

### Практические задания (1.5 часа)

**Задача 3.1:** Для иерархии `Shape` → `Circle`, `Rectangle` создать метод `calculateArea()` и переопределить его в дочерних классах.

**Задача 3.2:** Создать класс `Phone` с методом `call()`. Создать `Smartphone extends Phone` и переопределить метод `call()`, добавив функциональность видеозвонка.

**Задача 3.3:** Создать класс `Transport` с методом `move()`. Создать классы `Car`, `Bicycle`, `Boat`, каждый со своей реализацией `move()`.

---

## 📅 День 4: Полиморфизм

### Теоретическая часть (2 часа)

#### Что такое полиморфизм?

- Определение полиморфизма
- "Много форм" одного интерфейса
- Полиморфизм во время выполнения (runtime polymorphism)

#### Переменная родительского типа

```java
Animal animal1 = new Dog();
Animal animal2 = new Cat();

animal1.makeSound();  // Гав-гав!
animal2.makeSound();  // Мяу!
```

#### Массивы полиморфных объектов

```java
Animal[] animals = {
    new Dog(),
    new Cat(),
    new Bird()
};

for (Animal animal : animals) {
    animal.makeSound();  // Каждый издает свой звук
}
```

#### Приведение типов

- Восходящее приведение (upcasting) — автоматическое
- Нисходящее приведение (downcasting) — явное
- Оператор `instanceof`

```java
Animal animal = new Dog();

if (animal instanceof Dog) {
    Dog dog = (Dog) animal;
    dog.wagTail();
}
```

### Практические задания (1 час)

**Задача 4.1:** Создать массив объектов `Shape[]` с разными фигурами. Пройти по массиву и вызвать метод `calculateArea()` для каждой.

**Задача 4.2:** Создать класс `PaymentMethod` с методом `pay(double amount)`. Создать классы `CreditCard`, `PayPal`, `Cash`. Использовать полиморфизм для обработки разных способов оплаты.

**Задача 4.3:** Реализовать систему сотрудников: `Employee` → `Developer`, `Tester`, `Manager`. Метод `calculateSalary()` реализовать по-разному для каждого типа.

---

## 📅 День 5: Практика и закрепление

### Комплексные задания (3 часа)

**Проект 5.1: Система животных в зоопарке**

- Базовый класс `Animal` (name, age, makeSound(), eat())
- Классы: `Mammal`, `Bird`, `Reptile` (промежуточные)
- Конкретные животные: `Lion`, `Elephant`, `Parrot`, `Snake`
- Создать зоопарк (массив животных) и реализовать методы:
  - `feedAll()` — покормить всех
  - `makeAllSounds()` — все издают звуки
  - `getAnimalsByType(String type)` — фильтрация по типу

**Проект 5.2: Система фигур с расчетами**

- Базовый класс `Shape` с методами `calculateArea()`, `calculatePerimeter()`
- Классы: `Circle`, `Rectangle`, `Triangle`, `Square`
- Создать программу, которая:
  - Создает массив фигур
  - Вычисляет общую площадь всех фигур
  - Находит фигуру с максимальной площадью
  - Выводит информацию о каждой фигуре

**Проект 5.3: Система банковских счетов**

- `BankAccount` (accountNumber, balance, owner)
  - Методы: `deposit()`, `withdraw()`, `getBalance()`
- `SavingsAccount extends BankAccount`
  - Добавить процентную ставку
  - Метод `addInterest()`
- `CheckingAccount extends BankAccount`
  - Добавить лимит овердрафта
  - Переопределить `withdraw()` с учетом овердрафта
- Создать банк с разными типами счетов

---

## 🎓 Самостоятельная работа

### Теоретические вопросы для закрепления

1. В чем разница между наследованием и композицией?
2. Почему конструкторы не наследуются?
3. Что произойдет, если не вызвать `super()` в конструкторе дочернего класса?
4. В чем разница между `@Override` и перегрузкой метода?
5. Можно ли переопределить приватный метод?
6. Что такое динамическое связывание?
7. Зачем нужен оператор `instanceof`?

### Дополнительные задачи

**Задача Д1:** Создать иерархию `Device` → `Computer`, `Phone`, `Tablet`. Реализовать методы `turnOn()`, `turnOff()`, `charge()`.

**Задача Д2:** Создать систему транспорта: `Vehicle` → `LandVehicle`, `WaterVehicle`, `AirVehicle` → конкретные виды транспорта.

**Задача Д3:** Реализовать простую игру с персонажами: `Character` → `Warrior`, `Mage`, `Archer`. Каждый с уникальными способностями.

---

## 📊 Критерии оценки знаний

### Минимальный уровень ✅

- Понимает концепцию наследования
- Может создать дочерний класс
- Использует `super` в конструкторах
- Переопределяет методы с `@Override`

### Средний уровень ⭐

- Проектирует иерархии из 2-3 уровней
- Понимает полиморфизм и использует его
- Правильно использует приведение типов
- Знает, когда применять `instanceof`

### Продвинутый уровень 🚀

- Проектирует сложные иерархии классов
- Эффективно использует полиморфизм для гибкости кода
- Понимает принципы SOLID (хотя бы Single Responsibility и Open/Closed)
- Может объяснить разницу между абстрактными классами и интерфейсами (подготовка к след. неделе)

---

## 🔗 Связь с автотестированием

**Почему это важно для QA Automation:**

1. **Page Object Model** использует наследование для базовых страниц
2. **Test Base Classes** — общие настройки для тестов
3. **Selenium WebDriver** работает через полиморфизм (разные браузеры)
4. **Frameworks** (TestNG, JUnit) используют наследование для test-классов

**Пример из тестирования:**

```java
class BasePage {
    WebDriver driver;

    BasePage(WebDriver driver) {
        this.driver = driver;
    }

    void clickElement(WebElement element) {
        element.click();
    }
}

class LoginPage extends BasePage {
    LoginPage(WebDriver driver) {
        super(driver);
    }

    void login(String username, String password) {
        // Использует методы BasePage
    }
}
```

---

## 📚 Рекомендуемые ресурсы

- Oracle Java Tutorials: Inheritance
- Head First Java: глава "Наследование и полиморфизм"
- Статьи про SOLID принципы (подготовка)

---

## ✅ Чек-лист недели

- [ ] Создал класс, наследующий другой класс
- [ ] Использовал `super` для вызова конструктора родителя
- [ ] Переопределил метод с аннотацией `@Override`
- [ ] Использовал полиморфизм (переменная родительского типа)
- [ ] Работал с массивом полиморфных объектов
- [ ] Использовал `instanceof` для проверки типа
- [ ] Выполнил минимум 2 комплексных проекта
- [ ] Могу объяснить разницу между наследованием и композицией

**📌 Итог недели:** Понимаю принципы наследования и полиморфизма, могу проектировать иерархии классов и эффективно переиспользовать код.
