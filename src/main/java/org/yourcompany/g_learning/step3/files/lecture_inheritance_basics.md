# 🎓 Лекция: Основы наследования в Java

## 📖 Введение

Представьте, что вы создаете систему для зоопарка. У вас есть львы, тигры, медведи, попугаи... Каждое животное имеет общие характеристики: имя, возраст, вес. Все животные могут есть, спать, издавать звуки. Но при этом у каждого вида есть свои уникальные особенности.

Было бы очень неудобно для каждого животного писать одни и те же поля и методы заново. Именно здесь на помощь приходит **наследование** — один из ключевых принципов объектно-ориентированного программирования (ООП).

---

## 🧩 Что такое ООП?

**Объектно-ориентированное программирование (ООП)** — это подход к написанию программ, при котором мы моделируем реальный мир с помощью объектов.

### Четыре основных принципа ООП

1. **Инкапсуляция** — упаковка данных и методов в единый объект, скрытие внутренней реализации
2. **Наследование** — возможность создавать новые классы на основе существующих
3. **Полиморфизм** — способность объектов принимать разные формы
4. **Абстракция** — выделение главных характеристик объекта, игнорирование деталей

### Простая аналогия

Представьте **чертеж дома** (класс) и **построенный дом** (объект).

- **Класс** — это план, описание того, как должно быть устроено что-то
- **Объект** — это конкретный экземпляр, созданный по этому плану

```java
// Класс - чертеж
class House {
    int rooms;
    String color;

    void openDoor() {
        System.out.println("Дверь открыта");
    }
}

// Объекты - конкретные дома
House myHouse = new House();
House neighborHouse = new House();
```

---

## 🌳 Принцип наследования

**Наследование** — это механизм, позволяющий создать новый класс на основе существующего, переняв его характеристики и добавив новые.

### Аналогия из жизни

Подумайте о семейном дереве. Вы наследуете определенные черты от родителей (цвет глаз, рост), но при этом имеете свои уникальные особенности.

В программировании:

- **Родительский класс** (superclass, базовый класс) — класс, от которого наследуют
- **Дочерний класс** (subclass, производный класс) — класс, который наследует

### Зачем это нужно?

1. **Переиспользование кода** — не нужно писать один и тот же код много раз
2. **Логическая организация** — создание иерархий классов
3. **Упрощение поддержки** — изменения в родительском классе автоматически влияют на дочерние
4. **Расширяемость** — легко добавлять новую функциональность

### Отношение "является" (IS-A)

Наследование описывает отношение "является". Если можно сказать "А **является** Б", то, вероятно, А должен наследоваться от Б.

- Собака **является** животным → Dog extends Animal
- Машина **является** транспортом → Car extends Vehicle
- Студент **является** человеком → Student extends Person

---

## 🔧 Базовый синтаксис наследования

### Ключевое слово `extends`

В Java для наследования используется ключевое слово `extends`:

```java
class РодительскийКласс {
    // поля и методы
}

class ДочернийКласс extends РодительскийКласс {
    // наследует поля и методы родителя
    // может добавлять свои
}
```

### Первый пример: Животные

```java
// Родительский класс
class Animal {
    String name;
    int age;

    void eat() {
        System.out.println(name + " ест");
    }

    void sleep() {
        System.out.println(name + " спит");
    }

    void makeSound() {
        System.out.println("Животное издает звук");
    }
}

// Дочерний класс
class Dog extends Animal {
    String breed;  // Новое поле, специфичное для собаки

    void wagTail() {  // Новый метод, специфичный для собаки
        System.out.println(name + " виляет хвостом");
    }

    void fetch() {
        System.out.println(name + " приносит палку");
    }
}
```

### Что происходит?

Класс `Dog` **автоматически получает**:

- Поле `name`
- Поле `age`
- Метод `eat()`
- Метод `sleep()`
- Метод `makeSound()`

И **добавляет свои**:

- Поле `breed`
- Метод `wagTail()`
- Метод `fetch()`

### Использование

```java
Dog myDog = new Dog();
myDog.name = "Шарик";
myDog.age = 3;
myDog.breed = "Лабрадор";

myDog.eat();        // Наследованный метод
myDog.sleep();      // Наследованный метод
myDog.wagTail();    // Собственный метод
myDog.fetch();      // Собственный метод
```

---

## 📦 Что наследуется, а что нет?

### ✅ Наследуются

- **Public поля и методы** — доступны везде
- **Protected поля и методы** — доступны в том же пакете и в наследниках
- **Package-private (без модификатора)** — доступны только в том же пакете

### ❌ НЕ наследуются

- **Private поля и методы** — доступны только внутри самого класса
- **Конструкторы** — каждый класс должен иметь свой конструктор

### Пример с модификаторами доступа

```java
class Parent {
    public String publicField = "Публичное";
    protected String protectedField = "Защищенное";
    private String privateField = "Приватное";

    public void publicMethod() {
        System.out.println("Публичный метод");
    }

    protected void protectedMethod() {
        System.out.println("Защищенный метод");
    }

    private void privateMethod() {
        System.out.println("Приватный метод");
    }
}

class Child extends Parent {
    void testAccess() {
        System.out.println(publicField);      // ✅ Работает
        System.out.println(protectedField);   // ✅ Работает
        // System.out.println(privateField);  // ❌ Ошибка!

        publicMethod();      // ✅ Работает
        protectedMethod();   // ✅ Работает
        // privateMethod();  // ❌ Ошибка!
    }
}
```

---

## 🏗️ Конструкторы в наследовании

### Проблема

Когда мы создаем объект дочернего класса, нужно инициализировать не только его поля, но и поля родительского класса. Как это сделать?

### Решение: ключевое слово `super`

**`super`** — это ссылка на родительский класс. Используется для:

1. Вызова конструктора родительского класса
2. Обращения к методам родительского класса
3. Обращения к полям родительского класса (если они перекрыты)

### Вызов конструктора родителя

```java
class Animal {
    String name;
    int age;

    // Конструктор родительского класса
    Animal(String name, int age) {
        this.name = name;
        this.age = age;
        System.out.println("Создано животное: " + name);
    }
}

class Dog extends Animal {
    String breed;

    // Конструктор дочернего класса
    Dog(String name, int age, String breed) {
        super(name, age);  // Вызов конструктора Animal
        this.breed = breed;
        System.out.println("Порода: " + breed);
    }
}
```

### Использование

```java
Dog myDog = new Dog("Шарик", 3, "Лабрадор");

// Вывод:
// Создано животное: Шарик
// Порода: Лабрадор
```

### ⚠️ Важные правила

1. **`super()` должен быть первой строкой** в конструкторе дочернего класса
2. Если вы не вызовете `super()` явно, Java автоматически вызовет `super()` без параметров
3. Если у родителя нет конструктора без параметров, вы **обязаны** явно вызвать `super(параметры)`

### Пример ошибки

```java
class Animal {
    String name;

    // Есть только конструктор с параметром
    Animal(String name) {
        this.name = name;
    }
}

class Dog extends Animal {
    String breed;

    // ❌ ОШИБКА! Java пытается вызвать super(), но такого конструктора нет
    Dog(String breed) {
        this.breed = breed;
    }
}
```

### Правильное решение

```java
class Dog extends Animal {
    String breed;

    // ✅ Явно вызываем super(name)
    Dog(String name, String breed) {
        super(name);
        this.breed = breed;
    }
}
```

---

## 🔄 Цепочка конструкторов

Когда создается объект дочернего класса, происходит **цепочка вызовов конструкторов** от самого верхнего родителя до конкретного класса.

### Пример иерархии

```java
class LivingBeing {
    LivingBeing() {
        System.out.println("1. Создано живое существо");
    }
}

class Animal extends LivingBeing {
    Animal() {
        super();  // Вызов конструктора LivingBeing
        System.out.println("2. Создано животное");
    }
}

class Dog extends Animal {
    Dog() {
        super();  // Вызов конструктора Animal
        System.out.println("3. Создана собака");
    }
}
```

### Создание объекта

```java
Dog myDog = new Dog();

// Вывод:
// 1. Создано живое существо
// 2. Создано животное
// 3. Создана собака
```

### Порядок инициализации

1. Вызывается конструктор самого верхнего предка
2. Инициализируются поля предка
3. Выполняется тело конструктора предка
4. Затем то же самое для следующего уровня
5. И так далее до самого дочернего класса

---

## 💡 Практические примеры

### Пример 1: Транспорт

```java
class Vehicle {
    String brand;
    int year;
    double maxSpeed;

    Vehicle(String brand, int year, double maxSpeed) {
        this.brand = brand;
        this.year = year;
        this.maxSpeed = maxSpeed;
    }

    void displayInfo() {
        System.out.println("Марка: " + brand);
        System.out.println("Год: " + year);
        System.out.println("Макс. скорость: " + maxSpeed + " км/ч");
    }

    void move() {
        System.out.println(brand + " движется");
    }
}

class Car extends Vehicle {
    int numberOfDoors;
    String fuelType;

    Car(String brand, int year, double maxSpeed, int numberOfDoors, String fuelType) {
        super(brand, year, maxSpeed);
        this.numberOfDoors = numberOfDoors;
        this.fuelType = fuelType;
    }

    void honk() {
        System.out.println("Биип-биип!");
    }

    void displayCarInfo() {
        displayInfo();  // Вызов метода родителя
        System.out.println("Количество дверей: " + numberOfDoors);
        System.out.println("Тип топлива: " + fuelType);
    }
}

// Использование:
Car myCar = new Car("Toyota", 2020, 180, 4, "Бензин");
myCar.displayCarInfo();
myCar.move();
myCar.honk();
```

### Пример 2: Сотрудники компании

```java
class Person {
    String name;
    int age;

    Person(String name, int age) {
        this.name = name;
        this.age = age;
    }

    void introduce() {
        System.out.println("Меня зовут " + name + ", мне " + age + " лет");
    }
}

class Employee extends Person {
    String employeeId;
    double salary;
    String department;

    Employee(String name, int age, String employeeId, double salary, String department) {
        super(name, age);
        this.employeeId = employeeId;
        this.salary = salary;
        this.department = department;
    }

    void work() {
        System.out.println(name + " работает в отделе " + department);
    }

    void displayEmployeeInfo() {
        introduce();  // Метод родителя
        System.out.println("ID: " + employeeId);
        System.out.println("Зарплата: " + salary);
        System.out.println("Отдел: " + department);
    }
}

class Manager extends Employee {
    int teamSize;

    Manager(String name, int age, String employeeId, double salary,
            String department, int teamSize) {
        super(name, age, employeeId, salary, department);
        this.teamSize = teamSize;
    }

    void conductMeeting() {
        System.out.println(name + " проводит встречу с командой из " + teamSize + " человек");
    }
}

// Использование:
Manager boss = new Manager("Иван Иванов", 35, "EMP001", 150000, "IT", 10);
boss.displayEmployeeInfo();
boss.work();
boss.conductMeeting();
```

### Пример 3: Банковские счета

```java
class BankAccount {
    String accountNumber;
    String ownerName;
    double balance;

    BankAccount(String accountNumber, String ownerName, double initialBalance) {
        this.accountNumber = accountNumber;
        this.ownerName = ownerName;
        this.balance = initialBalance;
        System.out.println("Счет создан для: " + ownerName);
    }

    void deposit(double amount) {
        if (amount > 0) {
            balance += amount;
            System.out.println("Пополнение: +" + amount + " руб. Баланс: " + balance);
        }
    }

    void withdraw(double amount) {
        if (amount > 0 && amount <= balance) {
            balance -= amount;
            System.out.println("Снятие: -" + amount + " руб. Баланс: " + balance);
        } else {
            System.out.println("Недостаточно средств!");
        }
    }

    void displayBalance() {
        System.out.println("Счет " + accountNumber + ": " + balance + " руб.");
    }
}

class SavingsAccount extends BankAccount {
    double interestRate;  // Процентная ставка

    SavingsAccount(String accountNumber, String ownerName,
                   double initialBalance, double interestRate) {
        super(accountNumber, ownerName, initialBalance);
        this.interestRate = interestRate;
    }

    void addInterest() {
        double interest = balance * interestRate / 100;
        balance += interest;
        System.out.println("Начислены проценты: +" + interest + " руб.");
        System.out.println("Новый баланс: " + balance + " руб.");
    }

    void displaySavingsInfo() {
        displayBalance();
        System.out.println("Процентная ставка: " + interestRate + "%");
    }
}

// Использование:
SavingsAccount savings = new SavingsAccount("SA-12345", "Петр Петров", 10000, 5.5);
savings.deposit(5000);
savings.addInterest();
savings.displaySavingsInfo();
```

---

## 🤔 Вопросы для самопроверки

1. **Что такое наследование в ООП? Приведите аналогию из реальной жизни.**

2. **Какие четыре основных принципа ООП вы знаете? Кратко опишите каждый.**

3. **Что такое отношение "IS-A"? Приведите 3 примера из реального мира.**

4. **Какое ключевое слово используется в Java для наследования? Напишите синтаксис.**

5. **Что наследует дочерний класс от родительского? Что НЕ наследуется?**

6. **Зачем нужно ключевое слово `super`? В каких случаях оно используется?**

7. **Почему вызов `super()` должен быть первой строкой в конструкторе?**

8. **Что произойдет, если у родительского класса нет конструктора без параметров, а в дочернем классе не вызван `super()` явно?**

9. **Объясните цепочку вызова конструкторов при создании объекта дочернего класса.**

10. **В чем преимущества использования наследования? Назовите минимум 3 причины.**

---

## 💻 Практические задачи

### Задача 1: Система фигур (легкая)

**Постановка:**  
Создайте класс `Shape` (фигура) с полями `color` (цвет) и `name` (название). Добавьте конструктор и метод `displayInfo()`, который выводит информацию о фигуре.

Создайте класс `Circle` (круг), наследующий `Shape`. Добавьте поле `radius` (радиус). Создайте конструктор, использующий `super`. Добавьте метод `calculateArea()`, который вычисляет площадь круга (π \* r²).

Создайте объект круга и вызовите оба метода.

**Подсказка к решению:**  
Начните с определения родительского класса `Shape` с базовыми полями. Убедитесь, что конструктор `Shape` принимает параметры для инициализации `color` и `name`. В классе `Circle` при вызове `super()` передайте значения для родительских полей, а затем инициализируйте собственное поле `radius`. Для вычисления площади используйте формулу: `Math.PI * radius * radius`. Метод `displayInfo()` из родительского класса можно вызвать напрямую из объекта `Circle`, так как он наследуется.

---

### Задача 2: Иерархия учебных заведений (средняя)

**Постановка:**  
Создайте класс `EducationalInstitution` (учебное заведение) с полями:

- `name` (название)
- `address` (адрес)
- `yearFounded` (год основания)

Добавьте конструктор и метод `displayBasicInfo()`.

Создайте класс `School` (школа), наследующий `EducationalInstitution`. Добавьте поля:

- `numberOfStudents` (количество учеников)
- `hasElementaryClasses` (есть ли начальные классы - boolean)

Создайте класс `University` (университет), также наследующий `EducationalInstitution`. Добавьте поля:

- `numberOfFaculties` (количество факультетов)
- `hasDoctoralProgram` (есть ли докторантура - boolean)

Для обоих дочерних классов создайте конструкторы и методы, выводящие полную информацию.

**Подсказка к решению:**  
Это задача на создание иерархии с одним родителем и двумя независимыми наследниками. Обратите внимание, что `School` и `University` — это два разных класса, оба наследующие от `EducationalInstitution`, но не связанные друг с другом. В конструкторах обоих классов первым делом вызывайте `super(name, address, yearFounded)` для инициализации родительских полей. Создайте метод `displayFullInfo()` в каждом дочернем классе, который сначала вызовет `displayBasicInfo()` родителя, а затем выведет специфичные для класса поля. Используйте булевы значения для условных выводов (например: "Есть начальные классы" или "Нет начальных классов").

---

### Задача 3: Система библиотеки (сложная)

**Постановка:**  
Создайте класс `LibraryItem` (библиотечный экземпляр) с полями:

- `id` (уникальный идентификатор)
- `title` (название)
- `author` (автор)
- `year` (год издания)
- `isAvailable` (доступен ли для выдачи - boolean)

Добавьте методы:

- `borrowItem()` — взять на время (меняет `isAvailable` на false)
- `returnItem()` — вернуть (меняет `isAvailable` на true)
- `displayInfo()` — вывод информации

Создайте классы-наследники:

**`Book`** с дополнительными полями:

- `numberOfPages` (количество страниц)
- `genre` (жанр)

**`Magazine`** с дополнительными полями:

- `issueNumber` (номер выпуска)
- `month` (месяц издания)

**`DVD`** с дополнительными полями:

- `duration` (продолжительность в минутах)
- `format` (формат, например "HD", "4K")

Для каждого дочернего класса создайте конструкторы и методы для вывода полной информации о конкретном типе экземпляра.

**Подсказка к решению:**  
Это комплексная задача на иерархию с тремя разными наследниками. Начните с тщательного проектирования родительского класса `LibraryItem`. Методы `borrowItem()` и `returnItem()` должны проверять текущее состояние: нельзя взять уже взятый предмет, нельзя вернуть то, что не бралось. Добавьте вывод сообщений о результатах операций.

В каждом наследнике конструктор должен принимать все параметры: как для родителя (id, title, author, year), так и специфичные для данного типа. Вызов `super()` должен инициализировать родительские поля, при этом `isAvailable` можно установить в `true` по умолчанию.

Создайте методы `displayDetailedInfo()` в каждом дочернем классе, которые сначала вызывают `displayInfo()` родителя, а затем добавляют специфичную информацию. Также можно добавить методы, специфичные только для определенных типов (например, `calculateReadingTime()` для книги на основе количества страниц).

---

### Задача 4: Система транспорта

**Постановка:**  
Создайте иерархию классов для транспортных средств. Базовый класс `Vehicle` с полями: `brand`, `model`, `year`, `fuelLevel`. Создайте наследников: `Car`, `Motorcycle`, `Truck`. Каждый должен иметь уникальные поля и методы.

---

### Задача 5: Домашние животные

**Постановка:**  
Разработайте систему для ветеринарной клиники. Базовый класс `Pet` с информацией о питомце. Наследники: `Dog`, `Cat`, `Bird` с породами, возрастом, особенностями ухода. Реализуйте методы для вакцинации и медицинской карты.

---

### Задача 6: Электронные устройства

**Постановка:**  
Создайте класс `ElectronicDevice` с полями: `brand`, `model`, `price`, `batteryLevel`. Наследники: `Smartphone`, `Laptop`, `Tablet`. Добавьте методы для зарядки, включения/выключения, вывода характеристик.

---

### Задача 7: Продукты питания

**Постановка:**  
Базовый класс `FoodProduct` с полями: `name`, `price`, `weight`, `expirationDate`. Наследники: `Dairy` (молочные), `Bakery` (выпечка), `Meat` (мясо). Реализуйте проверку срока годности и расчет цены со скидкой.

---

### Задача 8: Музыкальные инструменты

**Постановка:**  
Класс `MusicalInstrument` с базовыми характеристиками. Наследники: `StringInstrument` (струнные), `WindInstrument` (духовые), `PercussionInstrument` (ударные). Каждый со своими методами игры и настройки.

---

### Задача 9: Спортивные команды

**Постановка:**  
Создайте класс `Athlete` (спортсмен) с основной информацией. Наследники: `FootballPlayer`, `BasketballPlayer`, `TennisPlayer`. Реализуйте статистику, рейтинг, методы тренировки для каждого вида спорта.

---

### Задача 10: Недвижимость

**Постановка:**  
Базовый класс `Property` (недвижимость) с полями: `address`, `area`, `price`, `owner`. Наследники: `Apartment` (квартира), `House` (дом), `CommercialSpace` (коммерческое помещение). Добавьте методы расчета налогов, коммунальных платежей, стоимости аренды.

---

## 🎯 Заключение

Наследование — это мощный инструмент, который позволяет:

- ✅ Избежать дублирования кода
- ✅ Создавать логичные иерархии классов
- ✅ Легко расширять функциональность
- ✅ Моделировать реальный мир в коде

Освоив наследование, вы сделали важный шаг к пониманию объектно-ориентированного программирования. В следующих занятиях мы изучим переопределение методов и полиморфизм — концепции, которые раскрывают всю мощь наследования.

**Практикуйтесь!** Чем больше вы будете создавать классов и иерархий, тем лучше поймете, когда и как применять наследование.
